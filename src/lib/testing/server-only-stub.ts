// Stub for the "server-only" package when running under Vitest (plain Node,
// not Next's bundler, which is what normally enforces the server/client
// boundary that package guards). See vitest.config.ts resolve.alias.
export {};
